import * as magic_string from 'magic-string';

interface AnalyzeOptions {
    /**
     * @default true
     */
    rerenderTrace: boolean;
}
declare const analyzeOptionsDefault: {
    rerenderTrace: boolean;
};
type DeepRequired<T> = {
    [P in keyof T]-?: T[P] extends object ? DeepRequired<T[P]> : Required<T[P]>;
};
declare function analyzeCode(code: string, filename: string, options: AnalyzeOptions): {
    code: string;
    map: magic_string.SourceMap;
} | null;

export { AnalyzeOptions, DeepRequired, analyzeCode, analyzeOptionsDefault };
